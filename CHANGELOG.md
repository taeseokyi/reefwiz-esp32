# 변경 이력 (CHANGELOG)

**ReefWiz Controller C-1** 펌웨어의 판(version) 기록입니다. 최신 판이 맨 위입니다.

- 판 번호는 `MAJOR.MINOR.PATCH` —
  **MAJOR**: 데이터 형식·API 가 깨져 예전 대시보드/백업과 안 맞음 ·
  **MINOR**: 기능 추가 · **PATCH**: 버그 수정·문구·튜닝.
- 판을 올릴 때는 ①`src/version.py` 의 `VERSION`·`RELEASED` ②이 파일의 새 항목
  ③커밋 후 `git tag v<판>` — 세 가지를 같이 한다(`src/version.py` 헤더에 절차가 있다).
- 기기에 올라간 판을 확인하는 법: 정비페이지 맨 아래 **장비 정보** 카드,
  또는 `curl http://reefwiz.local/api/version`.
  판 뒤의 `+커밋해시` 는 배포 시점의 저장소 커밋이다(`+dev` = 손으로 올린 코드,
  `-dirty` = 커밋하지 않은 변경이 섞인 판).

---

## v1.3.0 — 2026-09-24

**설치 화면에서 다른 저장소 · 앱 교체** (mpy-webota v0.5.0)
- 설치 화면(`:8266/`)에 저장소 칸 — `https://github.com/owner/repo` 나 `owner/repo` 를 넣으면
  mpy-webota 를 쓰는 **공개 저장소 어디든** 그 Releases 의 패키지가 목록에 뜬다. 출처는
  여러 개(첫 항목이 기본), 빼기·기본으로.
- 다른 앱의 패키지는 **'앱 교체'** — 확인 후 설치, 새 앱이 90초를 못 버티면 코드·설정이 함께
  원래 앱(reefwiz-controller)으로 롤백. `/data` 와 토큰·WiFi 설정은 그대로. 되돌아올 때는
  이 저장소를 골라 다시 '앱 교체'.
- 기기 설정 `/webota.json` 의 출처가 `sources`(목록)로 — 옛 `packages` 도 읽는다.
- 시험·예제 앱: [mpy-webota-demo](https://github.com/taeseokyi/mpy-webota-demo).

## v1.2.0 — 2026-09-24

**배포 패키지 — 기기 화면에서 골라 바로 설치** (mpy-webota v0.4.0)
- 판마다 배포 패키지(`reefwiz-controller-v<판>+<커밋>.wpk`)를 GitHub Releases 에 올린다
  (`tools/release.sh`). 기기의 설치 화면 **`http://<기기>:8266/`** 에서 목록을 보고 골라
  '설치'하면 기기가 직접 내려받아 검증 → 배포 → 재부팅 → 시험 → 확인(또는 롤백).
  휴대폰으로도 된다. 옛 판도 목록에 쌓여 있어 골라서 되돌아갈 수 있다.
- **두 경로를 함께 쓴다**: 패키지 설치(운영자)와 WSL 세부 조정(`deploy.py --http`,
  `tools/webota.py put·get·rm·mv`). WSL 로 코드를 손대면 기기가 '+ 수동 변경 N' 으로
  표시하고, 패키지를 다시 설치하면 판 그대로 돌아간다. 이력은 한곳(`history`)에 쌓인다.
- `tools/deploy.py --pack DIR` — USB·원격과 같은 묶음으로 패키지를 만든다.
- 기기 설정(`/webota.json`)에 `app_id`·패키지 출처 — 다른 앱의 패키지는 거부한다.

## v1.1.0 — 2026-09-24

v1.0.0(`c0d7955`) 이후의 기능 추가와 수정. 핵심은 **USB 없는 원격 배포**다.

**원격 배포 · 파일 관리 (webota)**
- 범용 모듈 [mpy-webota](https://github.com/taeseokyi/mpy-webota) v0.2.0 도입(vendored,
  `tools/sync_webota.sh` 로 원본과 맞춘다). 별도 포트 :8266, 토큰 인증.
- `python3 tools/deploy.py --http 192.168.0.47` — 바뀐 파일만 올리고, 부팅 때 적용한다.
  새 판이 90초를 못 버티면 **자동 롤백**, 평시 앱 예외는 **구조 모드**(원격 배포만 살아 있음).
- 가드: 측정 중·모터 구동 중·회차 120초 전에는 배포 확정·리셋을 거부.
- 배포 라벨 `v<판>+<커밋>` 이 기기 이력(`tools/webota.py history`)에 남는다.
- `tools/webota.py` 로 기기 파일(소스·데이터) 조회·내려받기·수정·삭제.
- ★부팅 구조 변경: `main.py` 는 런처, 앱은 `app.py`, `boot.py` 는 배포 적용.

**도저**
- 하한(LRT_MIN)이 감량 권고를 증량으로 뒤집던 결함 수정 — 하한은 증량 방향에서만.
- 도징 자동 적용을 대시보드 스위치로 켜고 끈다(재배포 불필요).
- 값이 같아 명령을 보내지 않은 회차가 "(실패)"로 보이던 오표시 수정 → "(변경 없음)".
  표의 행을 탭하면 그 회차의 사유가 펼쳐진다(모바일에서도).

**BT · 정비페이지**
- 주변 BT 장치 검색(AT+INQ) — 연속 검색·중지, 광고 이름(RNAME), 측정 회차 보호 가드
  (`ops.guard_measure` 로 통일), HC-05 모드 관리 체계화(검색 후 AT 모드 갇힘 해소).
- 상대 장비의 `ver` 한 줄 수집·표시, 에어 분배기(Air A-1) 등록부 정정.
- 늦게 도착한 낡은 상태 응답이 화면을 과거로 되돌리던 문제, 장치 목록 레이아웃 수정.

**배포 도구**
- `tools/deploy_wsl.sh` — WSL 에서 `C:\Temp` 를 거쳐 USB 배포(첫 설치·복구용).
- 배포 스탬프가 dirty 까지 정확(WSL git), chart.js gzip 을 결정적으로(mtime=0).

## v1.0.0 — 2026-08-30

실장 운영에 들어간 첫 판. Windows PC + 작업 스케줄러로 돌던 reefwiz 를 ESP32-S3 단독
장비로 옮긴 결과이며, 측정 파이프라인이 실기에서 첫 완주(67분)를 마친 상태다.

**장비**
- 모델명 **ReefWiz Controller C-1**(코드 `RWC1`) 부여 — 계열은 측정기 `Meter M-1`,
  도징기 `Doser D-1`. 개체 시리얼은 MAC 뒤 3바이트.
- 장비 4종 공통 한 줄 규약 `<이름> v<판> #<빌드 커밋>` — 제어기는 `ver` 명령 대신
  `GET /api/ver`(한 줄 text/plain)·`/api/version` 의 `ver` 필드·정비페이지·부팅 로그로
  같은 줄을 낸다. 개체(MAC 뒤 3바이트)는 `/api/version` 의 `serial` 에 따로 있다.
- 버전 관리 도입: `src/version.py` 단일 진실 + 배포 스탬프(`buildinfo.py`) +
  `GET /api/version` + 화면 표시(정비페이지 장비 정보 카드 · 대시보드 푸터) +
  부팅 로그 첫 줄 + 백업 번들의 `device` 메타.

**측정·도저**
- dKH 측정 1회(사전폭기 → 평탄 판정 → 판독)와 CO₂ 편향 의심 판정, 에러 래치.
- 도저 자동 조정(7일 창 수준·추세), 수동 mL/일 오버라이드, 안전 레일(일 변화 상한·롤백).
- 측정 회차·도저 조정 회차를 정비페이지에서 변경(파일 우선, `config.py` 폴백).
- 측정 보류(정비 래치) — 정비·수질 안정화 동안 정시 측정을 의도적으로 멈춘다.

**연결·하드웨어**
- HC-05 1개를 측정기·도징기들에 번갈아 붙인다(AT+BIND 전환 + 응답 서명 신원 검증,
  불일치 시 링크 동결). STATE 핀으로 RF 연결 즉시 감지.
- 온보드 RGB LED(GPIO48) 경고등, 워치독(120초), WiFi 를 측정 스레드에서 분리한 netmaint.

**웹·저장**
- 대시보드(`/`)와 정비·조치 페이지(`/ops.html`) — 화면 없는 장비의 유일한 조작 경로.
- 14일 보존(날짜 기준) + 플래시 아카이브, 설정 백업·복원, WiFi 설정(AP 폴백 포함).
- 로그: 측정 1회 전체 조회(4000줄) + `tail -f` 증분 갱신.
