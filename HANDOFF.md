# 작업 인계 (2026-08-24 — 접합부 결함 2건 수정 / 2026-08-21 — 스케줄·다중 도징기 관리 / 2026-08-19 — 보드 확정 · 화면/SD 제거 · 장기 저장소 · 원본 대조 감사 · 정비 UI 재구성)

다음 세션에서 이어서 작업하기 위한 현재 상태 요약. 상세 설계·배선·API 는 README.md 참조.

## 저장소

- **GitHub(private): https://github.com/taeseokyi/reefwiz-esp32** — **2026-08-24 `f477c15`
  + 이 문서 갱신 커밋까지 푸시 완료**(로컬·원격 동일, 미푸시 커밋 없음)
  (보드 확정·화면/SD 제거 → 배선 도면 → 펌웨어 동봉 → 장기 저장소 → 시각 유지·RTC 미채택
  → **원본 대조 감사**(`b081317`) → **정비 UI 재구성 6연속 커밋**(`8631d93`~`dfc9a76`)
  → **스케줄·다중 도징기**(`86e4aa9`) → 시계 동기 버튼 제거(`fb97c63`)
  → **접합부 결함 2건**(복원 API `e19e9b4` / 시각 게이트 `f477c15`))
- **2026-08-21 작업분**: 측정 회차 설정 + 다중 도징기 시계 동기 — 새 모듈
  `src/schedule.py`·`src/devices.py`, 새 API `/api/schedule`·`/api/devices`(종전 `/api/bt`
  대체), 정비페이지 '스케줄' 카드 + '장치 목록'. 아래 '스케줄·다중 도징기' 절 참조.
- **★2026-08-24 작업분**: 검증 하네스가 닿지 않던 **접합부 결함 2건**을 고쳤다 —
  복원 API 이중 파싱(`e19e9b4`) · 시각 게이트(`f477c15`). 결함마다 **수정 + 그것을 고정하는
  회귀 테스트**를 한 커밋에 묶었다. 아래 '접합부 결함 수정' 절 참조.
- **다음 세션은 여기부터**: 코드·문서·테스트는 정리된 상태이고 남은 일은 **실장 검증뿐**이다
  (아래 TODO). 하드웨어 없이 할 수 있는 것은 다 했다 — 새 기능을 더 얹기보다, 보드를 받아
  펌웨어를 굽고 **장치 목록에 BIND 주소를 넣어** 실제로 붙여 보는 것이 다음 단계다.
  ★검증 재현: `/e/cygwin64/bin/python3 tools/test_measure_sim.py`(173체크, 약 6분) +
  `tools/test_archive.py`(42체크). 이 PC 의 `python3` 은 Store 스텁이라 cygwin 쪽을 쓴다.
  UI 는 `tools/devserver.py --port 8123` → `http://localhost:8123/ops.html`.
- 로컬 경로: `E:\cygwin64\home\ower\work\reefwiz-esp32`
- 원본(public): https://github.com/taeseokyi/reefwiz — **2026-08-18 시점까지 반영 완료**
- git 자격증명은 Windows 에 캐시됨. git 사용자는 **저장소 로컬**로만 설정
  (taeseokyi / tsyi@kisti.re.kr) — 전역 설정 건드리지 않음

## 무엇을 만들었나

원본의 Windows PC + 작업 스케줄러 + WSL 동기화 + GitHub Pages 구성을
**ESP32(MicroPython) 한 대로 대체**.

| 모듈 | 내용 |
|---|---|
| `measure.py` | KH 측정 V4 이식 — 평탄 추종, 이송 전제조건 검증, 액체 위치 추적, 비상정리, 에러 래치, 호스트 구제 |
| `link.py` | **HC-05 1개** 링크 — 대상 전환(AT 재바인드) + ★신원 검증, keepalive/재연결/재송신, 전원 재투입 하드리셋 |
| `dkh_dat.py` | dkh.dat 한 줄의 파싱·포매팅 단일 규약 (날짜 컬럼) |
| `doser.py` | 도저 조정 — Theil-Sen(실측 시간 간격), 스텝캡/데드밴드/정지유지, 에코검증→refresh→롤백 |
| `ops.py` | 조치 도구 — 래치해제·측정중단·측정정리·KCl강제·액체위치지정·명령콘솔·BT연결점검·**BT 대상 전환** (전부 웹) |
| `webserver.py` | LAN 전용 웹서버 — 대시보드·정비페이지·`/api/*`, plateau JSONL 스트리밍 |
| `wifinet.py` | WiFi 설정·AP 폴백(`reefwiz-setup` / `reefwiz1234`) |
| `datalog.py` | dkh.dat + 대시보드 JSON + plateau JSONL + CO₂ 편향 판정 |
| `archive.py` | ★장기 저장소(SD 대체) — 플래시 아카이브·설정 스냅샷·백업/복원, 용량 백스톱 |

## 확정된 사용자 결정 (변경 금지 — 재논의 불요)

1. **HC-06 유지**(장비 배선 무변경).
2. **★HC-05 1개**(2026-08-18 확정) — 두 장비에 `AT+BIND` 를 바꿔가며 번갈아 접속.
   매번 새로 연결해도 되고 전환 속도는 요구사항이 아니다(고속 1~2초 / 폴백 8~12초).
   *종전의 "장비당 전용 모듈 2개" 안을 대체한다.*
   내장 BT 는 불가: 공식 MicroPython 이 BT Classic/SPP 를 안 열어주고, Classic 은 구형
   ESP32 에만 있어 보드 업그레이드 계획(#11)과 충돌한다.
3. **LAN 전용 발행** — 외부 접근·GitHub Pages 미러 불필요
4. **MQTT(reefCore) 발행 제거**
5. 설정 저장은 GitHub API 커밋 → **로컬 POST**(토큰 불요)
6. **모든 정보 14일 보존** — ★회차 컷이 아니라 **날짜 컷**(기준일 = 마지막 기록일)
7. **UI 전부 한국어** — 정비페이지 버튼: 측정 / 측정 중단 / 측정 정리 / 래치해제 / BT 전환 / 로그
8. (폐지) 화면 줄바꿈 규칙 — 장비 화면을 없앴으므로 해당 없음
9. 용어 통일: **측정 챔버 / 홀딩 챔버 / KCl 보관액 / 위즈 수조**
10. 에러 시 **BT 연결 회복 후 명령 콘솔로 직접 정리**하는 워크플로 중시
11. **★보드 확정(2026-08-18)** — VCC-GND Studio **ESP32-S3 N16R8**(디바이스마트 VND019,
    21,000원): 16MB 플래시 + **8MB 옥탈(OPI) PSRAM**. 펌웨어는 MicroPython
    **`ESP32_GENERIC_S3` SPIRAM_OCT 변종**이어야 한다(일반 이미지는 PSRAM 미인식/부팅 루프).
    *종전의 "더 고성능 보드로 교체 예정"을 확정으로 대체한다.*
12. **★디스플레이·터치 제거(2026-08-18)** — 확인·조치는 **웹으로만**(대시보드 + `/ops.html`).
    `display.py` / `display_driver.py` / `kfont.bin` / 폰트 도구 전부 삭제.
    *종전의 "화면 한국어 UI" 요구를 대체한다.*
13. **★SD 카드 제거(2026-08-18)** — `storage.py` 삭제, 로그 전문 미러·RF 원장 폐지.
    RF 이벤트는 `measure_kh.log` 의 `[rf] ` 줄로 남는다.
    *같은 날 오전에 추가했던 "SD 리더 선택 장비" 결정을 대체한다.*
14. **★장기 저장소 = 플래시 아카이브 + 내려받기(2026-08-19)** — SD 를 뺀 자리를 `archive.py`
    (`/data/archive`, 파일별 2MB 상한·여유 1MB 하한에서 자동 축소)와 웹 백업
    (`/api/backup`·`/api/files`·`/data/<경로>`·`POST /api/restore`), PC 스크립트
    `tools/backup.py`(`--http` LAN / `--usb` mpremote)로 대체했다.
    ★**USB 저장소는 불가**(재조사 불요): MSC 디바이스는 esp32 포트 미구현(micropython#8426),
    USB 호스트는 MicroPython 전체 미구현(discussions#15477) — 둘 다 TinyUSB C 코드로 커스텀
    펌웨어를 빌드해야 해서 "펌웨어 무수정" 전제를 깬다. 대신 USB 케이블로 뽑는다(mpremote).
    ★복원은 **설정만** — 측정 데이터를 되돌리면 도저 수준·추세가 튄다.
15. **★RTC 모듈 추가하지 않는다(2026-08-19, 사용자 결정)** — 시각은 **NTP 만**으로 유지한다
    (부팅 시 + 하루 1회 재동기화, UTC 저장·표시할 때만 KST 변환). 정전 후 WiFi 가 안 붙으면
    `ntp_done` 게이트가 **정시 측정을 건너뛴다** — 시계 없이 측정하는 것보다 안전하다
    (수동 측정은 정비페이지에서 언제든 가능).
    사용자 원칙: **"가장 심플한 것이 견고하다"** — 부품·배선을 늘리는 개선안은 제안하지 말 것.
    DS3231 등 RTC 검토는 끝났고 재논의 대상이 아니다.
16. **★스케줄·장치를 웹에서 관리(2026-08-21, 사용자 요구)** — 측정 회차(시각·횟수)와 도저
    자동조정 회차는 `/data/schedule.json`, 장치(측정기 1대 + 도징기 N대)의 주소·이름·시계
    동기 시각은 `/data/devices.json`. 둘 다 `config.py` 보다 우선하고 정비페이지에서 고친다.
    **측정 회차 최소 간격은 2시간**(사용자 확정: "측정이 2시간을 넘긴 적이 없다") — 자정을
    넘는 간격도 원형으로 검사한다.
17. **★추가 도징기는 시계 동기 전용(2026-08-21, 사용자 확정 — 재논의 불요)** — 도저 펌웨어의
    응답 서명이 모든 도징기에서 동일해(`ls` → "왼쪽 동작") 신원 검증이 **도징기끼리는 구분하지
    못한다**. `set time` 은 값이 같아 주소가 뒤바뀌어도 무해하지만 `lrt`(도징량)는 오장비
    사고가 된다 → dKH 자동조정·수동 mL/일·lrt 적용은 **기본 도징기(id=`doser`, 목록 맨 위)
    1대**에만 허용한다(`ops._require_primary_doser`). 시계 동기만은 자동 전환을 허용한다.

**원본의 안전 레일은 반드시 유지** (실제 사고로 얻은 것들): 이송 전 airoff·ton 응답 검증
(거짓 성공 방지), 액체 위치 불명 시 동결, 비상정리 KCl 소크, 에러 래치, 평탄 미도달 음수 표식,
호스트 구제 계산.

## ★HC-05 1개 — 오장비 명령을 막는 5중 레일

"바인드가 어긋나면 도저 명령이 측정기로 간다"가 이 구성의 유일한 위험이었고, 코드로 막았다.
원본의 "거짓 성공은 있으면 안 된다" 원칙을 링크 계층에 적용한 것이다.

1. **신원 검증** — 전환 후 첫 구동 명령 전에 부작용 없는 조회로 응답 서명을 확인
   (측정기 `status` → `============` / 도저 `ls` → `왼쪽 동작·왼쪽 휴지`). 다른 장비 서명이
   오면 **즉시 동결**하고 `send()` 가 `LinkFrozen` 을 던진다. **자동 해제 없음** — 배선·BIND
   주소 확인 없이 풀면 같은 오접속을 반복하므로 정비페이지의 명시적 '동결 해제'로만 푼다.
2. **교차 프로브** — 침묵이 오면 다른 장비의 조회 형식으로 한 번 더 묻는다. 장비마다 줄 종단이
   달라(측정기 CRLF / 도저 LF only) 오접속이 '무응답'으로만 보이는데, 원인이 '상대 전원 꺼짐'
   인지 'BIND 주소 뒤바뀜'인지에 따라 운영자가 할 일이 완전히 다르다.
3. **모터 구동 중 전환 금지** — 전환은 곧 라디오 전원 차단이다. 모터가 도는 중에 끊으면 정지
   명령을 보낼 수단이 사라져 시약이 계속 주입된다.
4. **★측정 중 전환 금지(2026-08-19 추가)** — 회차 도중 대상을 바꾸면 이어지는 측정 명령이
   도저로 간다. `state.measuring` 인 동안 `select_target()` 은 거부하고 **force 로도 안 뚫린다**
   (바꾸려면 '측정 중단'이 먼저). 측정 흐름 자신(`measure.make_link` → `allow_measuring=True`)만
   지나가고, 이미 붙은 대상 재요청은 라디오를 안 건드리므로 통과한다. 정비페이지 버튼도 잠긴다.
5. **★도징량 경로를 1대로 제한(2026-08-21 추가)** — 위 ①②가 보장하는 것은 "요청한 **종류**가
   응답했다"까지다. 도징기 여러 대는 응답 서명이 같아 **원리적으로 구분할 수 없으므로**
   `lrt`(도징량)를 보내는 경로를 기본 도징기 1대로 묶었다(`_require_primary_doser`).
   `set time` 은 값이 전 도징기 동일해 무해하므로 예외로 자동 전환을 허용한다.
   ※같은 종류의 서명을 '남의 것'으로 보면 안 된다 — `_other_sigs` 가 종류 기준인 이유다
   (아니면 도징기 2대 구성에서 정상 응답이 곧 오접속 판정이 되어 전환이 매번 동결된다).

## 전환 방식 — 고속 경로(기본) + 전원 폴백

데이터시트(ZG1643) 확인 결과, 전원을 끊지 않고 **KEY 핀만으로** AT 모드에 들어갈 수 있다
(Way 1 — 보레이트는 통신값 9600 그대로). `AT+DISC` → `AT+BIND` → `AT+LINK` 으로 붙이고
KEY 를 내리면 끝이라 회당 1~2초다. `config.BT_SWITCH_MODE`:

- `"auto"`(기본) — 고속 시도 후 실패하면 전원 경로로 폴백
- `"key"` — 고속만 / `"power"` — 전원 재투입만(Way 1 미지원 펌웨어용, 기존 검증 경로)

BIND 와 LINK 을 둘 다 보낸다: BIND 는 *다음 자동 연결 대상*을 기억시키고, LINK 는 *지금*
붙인다. 하나만 쓰면 재부팅 후 엉뚱한 상대로 가거나(BIND 누락) 지금 안 붙는다(LINK 누락).

★어느 경로로 붙었든 끝에서 **신원 검증을 똑같이 통과**해야 한다 — 오장비 명령 위험은
경로 선택과 무관하다. 전원 경로는 AT 조차 응답 않는 좀비 상태의 최후 복구 수단이라 남긴다.

## 자동 연결

BIND 주소만 넣으면 조작 불요. 부팅 시 측정 장비로 자동 연결하고(main.py), 이후 measure /
doser 가 각자 필요할 때 `link.acquire()` 로 전환한다. 이미 그 대상이면 즉시 반환하므로 연속
측정에 전환 비용이 없다. 정비페이지의 'BT 전환' 버튼은 복구용이지 평상시용이 아니다.

## ★원본 대조 감사와 수정 (2026-08-19)

원본을 새로 클론해 측정·관리 기능을 파일 단위로 대조했다(원본은 2026-08-18 이후 **코드 커밋
없음** — `ae40963` 은 데이터 동기화라 대조 기준 불변). 측정 시퀀스·평탄 판정·안전 레일·도저
계산은 원본과 일치했고, 아래를 고쳤다.

| 발견 | 고친 것 |
|---|---|
| **`/api/dkh` 가 ref_kh 를 반환**(`lines[-1][4]` 위치 인덱싱 — 날짜 컬럼 때문에 한 칸 밀림). 원본은 `row["tank_kh"]` | `datalog.last_dat_row()` 신설 → 파서 경유. `webserver.py`·`tools/devserver.py` 둘 다. 회귀 테스트 고정(시나리오 F) |
| **로그가 무한 증식** — 회전 검사가 부팅 시 1회뿐. 원본은 측정마다 프로세스가 새로 떠 하루 3회 회전했지만 ESP32 는 상시 가동이라 핸들이 열린 뒤 상한을 무시했다(플래시 잠식 → `archive.guard` 가 아카이브를 대신 깎음) | `datalog.log` 가 쓴 바이트를 세어 **쓰는 도중에도** 상한에서 새로 시작. `archive.guard()` 도 하루 1회 실행(종전 부팅 시 1회) |
| **수동 우선 규칙 누락** — 원본 `doser_adjust.main()` 은 새 오버라이드를 적용하면 return 해서 그 회차 자동 조정을 건너뛴다. 이식본은 둘을 무조건 이어 불렀다(AUTO_APPLY 를 켜면 방금 넣은 수동값을 덮어씀) | `doser.post_measure(hour)` 신설(원본 main 순서 그대로), `main.py` 가 이걸 호출 |
| **도저 시계 동기화 미이식** — 원본은 스케줄러가 매일 `set_time.py doser` 로 `set time HH:MM:SS` 송신 | `doser.sync_clock()` — 하루 1회 틱(측정 중이면 종료 후) + 정비페이지 '시계 동기화' 버튼 |
| **calref 실행 경로 없음** — `ops._job_calref` 는 있으나 UI 버튼이 없어 CLI 를 없앤 뒤로 사실상 실행 불가 | `/ops.html` 에 수조 실측 dKH 입력란·'참조 교정 시작' 버튼(0.5~30 범위 검증) |
| 대시보드 도저 설명이 폐기된 회차 근사("최근 7일 **21회**") | 원본 문구(날짜 기준·실제 측정 시각 간격)로 교체 |
| `index.html` 초기 렌더 후 남은 `click()` 잔재 | 제거(4a8fafc 취지는 이미 반영돼 있었다) |
| `link.send()` 가 통신 예외를 재시도 없이 전파 | 원본 정책대로 `SEND_RETRY_MAX` 까지 재시도 후 마지막에만 raise |
| `reconnect()` 의 전원 펄스에 모터 가드 없음(전환·리셋에는 있음) | 모터 구동 중에는 펄스 생략(정지 명령 경로 보존), 마지막 시도에서만 허용 |
| 원본 `--dry-run`(무접속 계산 미리보기) 미이식 | `doser.preview()` + 정비페이지 '권고 미리보기' 버튼 |

**미이식으로 남긴 것(의도)**: `kma_humidity.py`(기상청 API 키가 필요한 습도 진단 — LAN 전용
기기에 외부 API·비밀키를 들이지 않는다), `bt_health.py` 의 연속 핑 모니터(단일 스레드라 측정과
충돌하고, RF 분포는 로그의 `[rf]` 줄이 이미 준다 — 1회 점검은 '연결 점검' 버튼), 분석 스크립트
(`plateau_analyze`·`test_aeration_*`·`test_co2_flag` — PC 에서 내려받은 데이터로 돌리면 된다).

### 사용자 요청 보완 (같은 날)

| 요청 | 반영 |
|---|---|
| 대시보드에서 정비페이지 링크가 안 보임 | 링크 자체는 문서 링크 목록에 있었지만 스크롤해야 보였다 → **헤더(히어로) 안 알약형 버튼**으로 상단 고정 |
| 정비페이지에 BT 연결 상태 표시(도저인지 측정기인지·연결 상태) | **'BT 연결' 전용 카드** 신설: 현재 대상·신원 검증 여부·마지막 응답 확인 시각·최근 RF 이벤트(한국어)·STATE 핀·전환 가능 여부·BIND 주소 설정 여부 + 배지. `link.status()` 를 확장했고(**UART 미접촉** — 상태 폴링은 웹 스레드다) 실제 생존 확인은 '연결 점검' 버튼 몫. 전환·점검·리셋 버튼도 이 카드로 모았다 |
| 측정 중 BT 연결 전환 금지 | `Link.select_target()` 에 측정 중 게이트 추가(레일 #4) — **force 로도 안 뚫린다**. 정비페이지 전환·리셋 버튼은 측정 중·모터 구동 중 **비활성**으로 그려진다 |
| 정비페이지를 작업 흐름대로 재구성 + BT 접속 정보 설정 | ①**액체 위치 지정을 '측정 정리'와 같은 행**으로(정리 레시피를 고르는 값이라 하나의 절차다). ②**'KCl 강제 공급' 삭제** — 챔버 상태를 모르는 채 시약을 미는 조작이라 의미가 없다(`_job_cleanup` 의 force 분기도 제거). ③**명령 콘솔·도징을 BT 연결 카드 하위로**(기본 접힘) — 콘솔의 장비 선택 드롭다운을 없애고 `ops.current_target()` 이 정한 **현재 대상**으로만 보낸다(빠른 명령도 대상별 세트). 도징은 **대상이 도저일 때만** 열린다(`_require_doser`). ④측정·참조 교정·정리 중에는 폴링을 1.5초로 올려 자동 전환 결과가 바로 보이게. ⑤**BT 접속 정보(BIND 주소)를 웹에서 설정** — `/data/bt.json`, MAC 형식 자동 변환, `config.py` 보다 우선, 백업 번들 포함 |
| 현재 상태를 명확히 / 명령 콘솔은 Idle 에서만 | `ops.device_state()` 신설 — measuring·job·frozen·latched·liquid_unknown·idle 을 **한 곳에서** 판정하고, 정비페이지 맨 위 상태 배너와 서버 게이트가 같은 결과를 쓴다. 명령 콘솔은 Idle 에서만 열리고, **측정 중에는 `ack` 로도 거부**(종전에는 cmd 만 큐잉을 허용해 "요청됨" 응답 뒤 몇 시간 있다 실행됐다). 래치·액체 위치 불명은 **원래 콘솔로 정리하는 상황**(결정 #10)이라 완전 차단 대신 운영자 확인 체크박스로 연다(해제 실행은 로그에 남는다). UI 는 입력·빠른명령 버튼까지 일괄 비활성 |
| '연결 점검'은 왜 안전한가 | 명령 자체는 부작용이 없었지만(측정기 `status`/도저 `ls` — 원본 bt_health 가 같은 명령으로 모니터했다) **구현이 읽기 전용이 아니었다**: `measure.make_link()`→`ensure_link()` 를 타서 ①대상이 도저면 측정기로 전환하고 ②무응답이면 `reconnect()` 가 전원 펄스를 최대 5회 줬다(= HC-05 리셋과 같은 위험을 확인 없이). → **지금 붙어 있는 대상에게 조회 1회만** 보내도록 고쳤다(전환·전원 펄스 없음, 무응답이면 사실만 보고하고 리셋 버튼을 안내). 신원 검증은 그대로 살아 있다(다른 장비 서명이면 즉시 동결). 시뮬레이터의 전원 토글 카운터로 '전원을 끊지 않는다'를 테스트로 고정했다. 겸사겸사 조치 도구의 측정기 링크 확보를 `measure.make_link()`(측정 전용, `allow_measuring=True`)에서 분리해 레일 안쪽으로 넣었다 |
| 'HC-05 리셋'이 무엇이며 강제 수행이면 위험을 알려야 한다 | 버튼에 **위험 문구 확인 대화상자**(펌프 소리 확인 질문)를 달고 빨간(위험) 스타일로 바꿨다. BT 카드에 '연결 점검 vs HC-05 리셋'의 차이·부작용을 적었고, `ops._job_hc05_reset` 에 **측정 중 거부** 가드를 추가(웹 계층 외 이중 방어)했다. 실행 시 로그를 남기고, 결과 메시지도 복구/동결/무응답으로 나눠 보고한다 |

### 스케줄·다중 도징기 (2026-08-21)

사용자 요구: ①측정 회차·횟수를 바꿀 수 있어야 한다(최소 간격 2시간) ②도징기가 여러 대일 수
있으므로 BT 주소를 개별 기억하고 시계 동기 스케줄을 장치별로 추가할 수 있어야 한다.

| 만든 것 | 내용 |
|---|---|
| `schedule.py` | 측정 회차·도저 조정 회차(`/data/schedule.json` 우선, config 폴백). 검증: 1~12개·0~23시·중복 불가·**원형 인접 간격 ≥2h**·조정 회차는 목록 안. 회차 판정(`due_measure`)과 보관 백스톱(`rows_cap`)도 여기 |
| `devices.py` | 장치 레지스트리(`/data/devices.json`) — 측정기 1대 + 도징기 최대 4대. 종류(`kind`)가 probe·서명·줄종단을 정하고, 장치는 이름·주소·`sync_hours` 를 갖는다. 구형 `bt.json` 은 읽어서 마이그레이션. 순서가 id 를 정한다(첫 도저 = 기본) |
| `link.py` | `TARGETS` 를 레지스트리에서 **제자리 재구축**(`refresh_targets`). ★`_other_sigs` 를 **종류 기준**으로 바꿨다 — 종전 '자신 외 전부' 규칙이면 도저2 를 등록하는 순간 도저 응답이 곧 오접속 판정이 되어(`_ask` 는 theirs 를 mine 보다 먼저 본다) **전환이 매번 동결**됐다 |
| `main.py` | 회차 판정을 `schedule.due_measure` 로. ★**긴 회차가 다음 회차를 잡아먹지 않게** 종료 시각의 슬롯도 소비한다(간격 2h 를 허용하면 05시 회차가 07:30 에 끝나는 순간 07시 회차가 곧바로 시작된다). 도징기 시계 동기를 '하루 1회 틱'에서 떼어내 **장치별 시각 루프**로 옮기고 `ntp_done` 게이트를 걸었다(종전에는 NTP 실패 시 2000-01-01 을 도저에 밀어 넣었다) |
| `datalog.py` | `SERIES_MAX`/`PLATEAU_MAX`(고정 `14×6`=84) → `schedule.rows_cap()`. 고정값은 하루 3회 전제라 **하루 6회를 넘기면 14일 창 안의 데이터가 행수 컷에서 잘렸다**(하루 12회면 168행 → 84행) |
| `webserver.py` | `/api/schedule`(GET/POST) 신설, `/api/devices` 가 종전 `/api/bt` 를 대체. 백업 복원 후 캐시 무효화(안 하면 복원값이 다음 재부팅까지 안 먹는다) |
| `ops.html` | **스케줄 카드**(24개 시각 칩 + 조정 회차 select + 서버와 같은 규칙의 사전 검증), BT 카드의 '접속 정보 설정' → **장치 목록**(이름·주소·시계 동기 시각, 도징기 추가/삭제), **전환 버튼을 장치 수만큼 동적 생성**. ★시계 동기 수동 버튼은 **두지 않는다**(사용자 지시 — 측정기 동작 중에 실행돼선 안 되는 작업이다. 서버는 이미 측정 중 조치 작업을 전부 거부하지만, 누를 수 있는 버튼을 두면 오해를 준다). 작업 자체는 API 에 남아 있다 |
| 도징 섹션 잠금 범위 | ★사용자 지시(같은 날 후속): 도저2 에 붙어 있는데 **수동 mL/일·목표 dKH 가 동작했다** → 그 둘도 `data-dose` 로 잠갔다. 파일에 저장되는 값이라 기술적으로는 대상과 무관하지만 화면상 오해를 준다. **서버는 막지 않는다** — 같은 두 API 를 대시보드가 쓰고(`index.html:1055`·`1077`) 대시보드에는 BT 대상 개념이 없어 서버에서 막으면 그쪽이 죽는다. 추가 도징기에서도 장치 목록·명령 콘솔은 계속 쓸 수 있다 |
| 기본 도징기 명칭 | ★사용자 지시: "도저" → **"올포리프 도저"**(`devices.PRIMARY_DOSER_NAME`). id 는 `doser` 그대로다(이력·API·테스트가 그 이름을 쓴다). `KINDS["doser"]["label"]`("도저")은 **종류**를 가리키는 일반명이라 그대로 두고 교차 프로브 로그·둘째 이후 기본 이름("도저2")에 쓴다. 대시보드는 이미 "도저 자동 조정 (올포리프)"로 적혀 있어 표현이 일치한다 |
| `devserver.py` | 스텁이 `schedule`·`devices` 를 **기기 코드 그대로 import** 한다 — 검증 규칙을 베껴 두면 둘이 갈라진다(종전 `_norm_addr` 사본은 삭제) |

**교차 검증**: 같은 `data/dkh.dat`·`dkh_series.json` 으로 원본 `doser_adjust`(3.6 호환 패치본)와
이식본 `doser._window()` 를 나란히 돌려 **점 15 / CO₂ 제외 4 / 수준 7.8760 / 추세 -0.0307 완전
일치** 확인.

### ★접합부 결함 수정 (2026-08-24)

코드를 새로 읽어 보다가 **테스트가 닿지 않는 두 접합부**에서 결함을 찾았다. 둘 다 이식된
로직 자체가 아니라 계층 사이의 이음새이고, 그래서 159체크가 통과하는 동안 살아 있었다.

| 결함 | 무슨 일이 났나 | 고친 것 |
|---|---|---|
| **`/api/restore` 가 본문을 두 번 파싱** (`webserver.py`) | `_handle` 이 이미 dict 으로 파싱해 넘긴 본문을 `json.loads(body)` 로 다시 파싱 → **TypeError**(`except ValueError` 로는 안 잡힌다) → `_serve` 의 포괄 except 까지 올라가 **응답 없이 연결이 끊겼다**. 정비페이지 '설정 복원' 버튼은 **항상** 네트워크 오류였다(기기에서 한 번도 성공한 적이 없다) | 재파싱 제거 — `archive.restore(body)` 직행. 빈 dict(파싱 실패·본문 상한 초과)은 사유를 알린다. 번들 형식(kind) 검사는 `archive.restore` 한 곳 |
| **NTP 게이트가 'WiFi 는 붙었지만 NTP 실패'를 통과** (`main.py`) | `ntp_done = online` 이라 NTP 5회 실패 후에도 게이트가 열렸다(`ntp_sync()` 반환값을 버렸다). 인터넷 없는 공유기·UDP 123 차단 망에서 **2000-01-01 시계로 회차가 돌고**, 기본 동기 시각이 0시라 부팅 직후 `set time 00:00:xx` 가 나가 **도저 시계를 망친다** — 결정 #15 와 main 주석이 막겠다고 적어 둔 사고 그 자체 | 판정을 `rwtime.time_ready(online)`(= online **and** ntp_sync 성공)로. NTP 만 실패한 경우를 별도 문구로 알린다. 하루 1회 재동기 실패로는 게이트를 닫지 않는다(이미 맞은 시계의 드리프트는 측정 중단 사유가 아니다) |

**왜 아무도 못 잡았나 — 그게 이번 수정의 핵심이다.**

- 복원: `test_archive.py` 는 `archive.restore()` 를 **직접** 부르고, 개발 스텁은 복원 검증을
  **베껴서 재구현**해 뒀다. 그래서 스텁만 옳게 동작했고 HTTP 경로는 아무도 밟지 않았다.
  → `tools/devserver.py` 가 이제 `archive` 를 **기기 코드 그대로 import** 한다
  (`restore`·`bundle`·`status`·`CONFIG_FILES` 사본 삭제, 복원 후 캐시 무효화도 기기와 동일).
  2026-08-21 에 `schedule`·`devices` 에 적용한 원칙("사본을 두면 갈라진다")을 나머지에도 적용.
- NTP: `main.py` 는 **import 하는 순간 `main()` 이 돌아** 테스트가 부를 수 없었다.
  → 시각 판정을 `rwtime.ntp_sync()` / `rwtime.time_ready()` 로 옮겼다(시각의 주인은 rwtime
  이다). `ntptime` 은 **함수 안에서** import 한다 — rwtime 은 PC 테스트·백업 도구도 쓰므로
  최상단에 두면 그쪽 전부에 스텁을 강요한다.

**추가된 회귀 테스트 14건**(159 → **173**): `/api/restore` HTTP 경로(파싱된 본문 수용,
복원 즉시 회차 반영=캐시 무효화, 형식 불일치·빈 본문·간격 2h 미만 거부, 거부 시 무변경) +
시나리오 **H 시각 게이트**(NTP 성공/실패, 실패 시 게이트 닫힘, WiFi 미접속이면 시도조차
안 함, 재시도 상한, 호스트 설정). 하네스에도 `ntptime` 스텁(호출 횟수 카운터)과
`config.ARCHIVE_DIR` 임시 경로 고정을 넣었다(종전에는 아카이브 경로가 `/data/archive` 절대
경로로 굳어 PC 파일시스템을 건드릴 수 있었다).

**남은 관찰(결함으로 단정하지 않음)**: `measure._liquid`(액체 위치)는 메모리에만 있고
`run_measurement` 가 매 회차 시작에서 `KCL/EMPTY` 로 리셋한다. 에러로 끝난 회차는 에러 래치가
다음 회차를 막지만 **운영자 중단은 의도적으로 래치를 걸지 않으므로**, 중단 후 비상정리까지
실패해 위치가 UNKNOWN 인 상태에서 다음 정시 회차가 그냥 시작된다(정비페이지는 '액체 위치
불명'을 보여 준다). 물리적으로 문제가 되는지는 실물 배관을 아는 쪽의 판단이라 손대지 않았다 —
막으려면 `due_measure` 게이트에 한 줄이면 된다.

## 검증 완료 (하드웨어 없이)

```bash
python3 tools/test_measure_sim.py      # ★173체크 ALL PASS (약 6분)
python3 tools/test_archive.py          # ★아카이브·백업/복원 42체크 ALL PASS (수초)
python3 tools/devserver.py --port 8123 # 스텁 서버 → 대시보드·정비페이지·API 계약 확인
```

- 시나리오 A~D(정상/RF 순단/에러 래치/조치 콘솔) + **E(BT 대상 전환·신원 검증)**
  + F(관리 계층·API 계약) + G(스케줄·다중 도징기) + **★H(시각 게이트 — NTP 실패는 성공이
  아니다, 2026-08-24)**
  — 시나리오 E 하네스는 HC-05 자체를 모델링한다: Way 1(전원 켠 채 KEY↑)·Way 2(KEY↑ 전원
  인가) 양쪽 AT 진입, AT+DISC/AT+LINK, 주소→포트 맵, 전원 토글 카운터. 그래서 "주소가
  뒤바뀐 상황", "고속 경로가 정말 전원을 안 끊는지", "Way 1 미지원 시 폴백"을 실제로 검증한다
- **도저 계산 교차 검증**: 원본 커밋 `1dd5020` 이 기록한 실측 출력
  (창 8/10~8/16 / 19점 / 수준 7.746 / 추세 -0.0430 per day)을 이식본이 완전히 동일하게 재현
- 대시보드 날짜 헬퍼 node 검증 12건, `_trim_dat`·에러 래치 12건, 보관 정책 8건 전부 통과
- 브라우저 확인: 경도·수조 pH 그래프 모두 활성 버튼(7일)과 일치하는 19점(8/12~8/18),
  "전체(14일)" 라벨, 도저 14행, 콘솔 오류 0

## 원본(upstream) 반영 상태 — 2026-08-18까지 완료

| 원본 커밋 | 내용 | 반영 |
|---|---|---|
| `bc3e763` + `1dd5020` | **dkh.dat 날짜 컬럼** 도입, 회차 근사 전면 폐기 | `dkh_dat.py` 신설, datalog·doser·대시보드·보관정책 전면 전환 |
| `a2282b6` | 수조 pH 초기 범위를 활성 버튼과 일치 | 반영 |
| `016d634` | 카드 제목에서 "· 최근 14일" 제거 | 반영 |
| `f02b820` | "전체" 버튼에 누적 일수 표기 | 반영(경도 그래프에도) |
| `4a8fafc` | 경도 그래프 초기 렌더 깜빡임 제거 | 반영 |
| `d885745` | 측정 소스에서 reefCore 자동 발행 제거 | 이미 반영됨(결정 #4) |

원본의 데이터 동기화 커밋(`chore: dKH 최신값 자동 갱신`, `data: 측정 동기화`)은 코드 변경이
아니므로 대상 아님. **다음에 이어갈 때는 원본에서 `2026-08-18` 이후 커밋만 확인하면 된다.**

## 남은 작업 (TODO)

1. **★BIND 주소 입력** — 실제 HC-06 주소를 넣어야 전환이 동작한다(비어 있으면 의도적으로
   거부 — 빈 주소로 아무 데나 붙는 것 방지). ★2026-08-21부터 **정비페이지 'BT 연결 →
   장치 목록'** 에서 넣는다(MAC 붙여넣기 가능). 소스 수정·재배포 불요이고,
   `config.BIND_ADDR_*` 는 폴백으로만 남는다. 도징기가 여러 대면 같은 화면에서 추가하고
   장치마다 시계 동기 시각을 준다 — **추가 도징기는 시계 동기 전용**이다(아래 결정 참조).
   실기에서 확인할 것: 장치의 '시계 동기' 칸을 **다음 정시**로 잠깐 바꿔 두고 그 시각에
   로그에 장치별 전환 + `set time` 이 각각 남는지(수동 버튼은 없앴다 — 위 표 참조),
   도저2 에 붙은 상태에서 도징량 조작이 거부되는지.
2. **보드 수령 후 확인** — `esptool ... flash_id` 로 ESP32-S3/16MB 확인 후 동봉 펌웨어
   (`firmware/`, v1.28.0 옥탈 변종)를 굽고 `esp.flash_size()`=16MB, `gc.mem_free()` 수 MB
   (=옥탈 PSRAM 정상 인식) 확인. 상세 절차는 README "펌웨어" 절. 상품 상세설명에 `YD-ESP32-C3 / RAM 512Kb` 라고 적힌 것은 판매처 오기이고
   벤더가 리비전 변경 가능을 명시했으므로, 실물이 정말 S3 N16R8 인지 직접 확인할 것.
3. **배선** — 능동 부품 0개. 기준 보드 = **ZS-040**(디바이스마트 VLT-BT018, 4,700원).
   ★도면: `docs/wiring-hc05.svg` (README "HC-05 배선" 절에 ASCII 판도 있다).
   ★핀은 S3 기준으로 재배정됐다(종전 25/26/32/33 은 S3 에서 못 쓴다 — 22~25 는 없는 핀,
   26~37 은 플래시·PSRAM 전용):
   - GPIO17(TX1) → RXD / GPIO18(RX1) → TXD
   - GPIO21 → **EN**. ZS-040 의 EN 은 온보드 LDO(LP2985) enable 이라 LOW 로 내리면 모듈
     전원이 꺼진다 → **MOSFET 스위치 불필요**. 220K 풀업이 있어 부동=ON, 풀다운도 불요
   - GPIO14 → **PIN34/PIO11**. ★헤더에 KEY 가 없다 — 온보드 버튼이 VCC→PIN34 로 물려
     있으므로 거기서 따야 한다(납땜 1군데). **10k 풀다운 권장** — 부팅 중 GPIO 가 뜬 채면
     모듈이 AT 모드로 부팅해 장비에 안 붙는다.
     ★**PIN34 = 코어 모듈 오른쪽 맨 위 패드**(부품면 위·6핀 헤더 아래로 놓았을 때 —
     사용자 확인 2026-08-24). 도면: `docs/hc05-pin34.svg`, README 'PIN34 는 어디인가' 절.
     버튼의 VCC 아닌 쪽 패드도 같은 네트라 그쪽에 납땜해도 된다(패드가 커서 쉽다).
     실물 확인은 **버튼을 누른 상태에서 VCC 와 도통되는 쪽**을 찾으면 된다
   - (선택) GPIO38 ← STATE(연결됨=HIGH, 순단 감지 가속)
4. **Way 1 확인** — 전원 켠 채 KEY 를 3.3V 로 올리고 **9600** 에서 `AT` → `OK` 가 오는지.
   오면 기본값(`BT_SWITCH_MODE="auto"`) 그대로 고속 경로가 쓰인다. 안 오면 `"power"` 로
   두고 전원 경로만 쓴다(이미 검증된 경로). README "실기기 확인 절차" 참조.
5. **실장 검증** — 전환 소요 실측(현재 타이밍 상수는 여유 있게 잡은 추정치),
   `RECONNECT_BACKOFF` 튜닝(로그의 `[rf] ` 줄이 실측 분포를 준다), 장시간 힙 모니터링
6. (선택) 아카이브·백업 링크를 대시보드에도 노출(정비 페이지에는 이미 있다)
   ※calref 버튼은 2026-08-19 감사에서 정비 페이지에 추가 완료
   ※하드웨어 추가(RTC·SD·화면)는 **전부 결정으로 배제됐다** — 결정 #12·#13·#15 참조.

## 배포 (실기기)

```bash
# ① 펌웨어(한 번만) — ★옥탈 PSRAM 변종, 저장소 firmware/ 에 동봉(v1.28.0, 수정 없이 그대로).
#    BOOT 누른 채 RESET → 다운로드 모드. erase_flash 는 /data 까지 지운다(먼저 백업).
esptool --chip esp32s3 --port COM3 erase_flash
esptool --chip esp32s3 --port COM3 --baud 460800 write_flash 0 firmware/ESP32_GENERIC_S3-SPIRAM_OCT-20260406-v1.28.0.bin
mpremote connect COM3 exec "import esp, gc; print(esp.flash_size(), gc.mem_free())"  # 16MB / 수 MB
# ② 코드·자산 — ★한 번에(2026-08-24 `tools/deploy.py` 신설). src/*.py→루트 · www/→/www ·
#    data/→/data 를 mpremote **1회 호출**로 올린다(gzip·디렉토리 생성 포함).
python3 tools/deploy.py --port COM3 --with-data    # 첫 설치
python3 tools/deploy.py --port COM3                # 이후 코드만(데이터 안 건드림)
```

`data/` 픽스처는 원본 실데이터의 최근 14일치(신형식)라 첫 설치에 올리면 도저 계산 이력이
이어진다. ★단 **이미 돌고 있는 기기에는 올리지 않는다** — 실측 dkh.dat·이력을 과거로
되돌리면 도저 수준·추세가 튄다. 그래서 `--with-data` 는 옵트인이다.

★수동으로 할 때 주의: `fs cp -r src :` 는 안 된다(`/src/main.py` 가 되어 부팅이 안 된다 —
루트의 `main.py` 를 실행하는 것이 MicroPython 규약이다). `www/`·`data/` 는 이름이 같아
`fs cp -r www data :` 로 폴더째 올라간다. mpremote 는 `+` 로 명령을 이어 붙일 수 있고
SHA256 이 같은 파일은 건너뛴다. PowerShell 에서는 `src/*.py` 글롭이 확장되지 않으므로
Git Bash 를 쓰거나 스크립트를 쓴다(상세: README '설치' 절).

WiFi 는 `config.py` 에 적거나, 미설정 시 AP `reefwiz-setup`(비번 `reefwiz1234`) →
`http://192.168.4.1/ops.html` 에서 설정. 정상 접속 후 `http://reefwiz.local`.

## 주의사항

- **`data/wifi.json` 은 .gitignore 영구 제외** — 실제 WiFi 자격증명, 장치에만 존재
- **dkh.dat 를 읽을 때 위치 인덱싱 금지** — 반드시 `dkh_dat.parse_parts()` 경유.
  `parts[4]` 같은 코드는 날짜 컬럼 때문에 한 칸씩 밀려 tank_kh 대신 ref_kh 를 반환한다
- **S3 핀 제약**: 26~37(플래시·옥탈 PSRAM), 19·20(USB), 43·44(UART0), 0·45·46(스트래핑),
  48(RGB LED)은 쓸 수 없고 22~25 는 존재하지 않는다. 여유 핀은 1~13·15·16·38~42·47
- 이 개발 환경의 파이썬은 **cygwin `/e/cygwin64/bin/python3` (3.6.4)** 이다.
  Git Bash 의 `python3` 는 Microsoft Store 스텁이라 동작하지 않는다 — PATH 를 앞에 붙여 쓸 것
- **mpremote 는 아직 이 PC 에 없다**(`pip install mpremote`). ★cygwin 3.6 에는 설치가 안 될
  수 있다 — 의존성 `platformdirs>=4.3.7` 이 파이썬 3.9+ 를 요구한다. mpremote 는 독립 CLI 라
  **다른(최신) 파이썬에 설치해도 무관**하다: `tools/deploy.py`(3.6 에서 동작)가 PATH 의
  `mpremote` 실행파일을 호출할 뿐이다. 보드 수령 전에 미리 깔아 두면 실장 당일이 편하다
- 에이전트는 `GIT_TERMINAL_PROMPT=0`, `GCM_INTERACTIVE=never` 라 push 인증 프롬프트를 띄울 수
  없다. 자격증명이 캐시됐으므로 이후 푸시는 문제 없음
